import { type CaptureEligibilityDecision } from "./contracts.js";
/**
 * Applies only the pure equality policy. Host authentication provenance is an
 * Event Every integration gate and is intentionally not established here.
 */
export declare function evaluateCaptureEligibility(input: unknown): CaptureEligibilityDecision;
//# sourceMappingURL=eligibility.d.ts.map